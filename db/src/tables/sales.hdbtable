COLUMN TABLE "com.sap.training.analytics::sales" (
  SALE_ID INTEGER NOT NULL,
  PRODUCT_ID INTEGER,
  CUSTOMER_ID INTEGER,
  STORE_ID INTEGER,
  DATE_ID DATE,
  QUANTITY INTEGER,
  AMOUNT DECIMAL(15, 2),
  DISCOUNT DECIMAL(5, 2),
  CURRENCY NVARCHAR(3),

  PRIMARY KEY (SALE_ID)
)